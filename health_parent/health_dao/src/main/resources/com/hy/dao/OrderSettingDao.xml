<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.hy.dao.OrderSettingDao">
    <insert id="insert" parameterType="OrderSetting">
        insert into t_ordersetting (orderDate,number,reservations)
        values (#{orderDate,jdbcType=DATE},#{number},#{reservations})
    </insert>

    <update id="update" parameterType="OrderSetting">
        update t_ordersetting
        set number = #{number}
        where orderDate=#{orderDate,jdbcType=DATE}
    </update>

    <select id="findByOrderDate" resultType="com.hy.pojo.OrderSetting">
        select * from t_ordersetting
        where orderDate=#{orderDate,jdbcType=DATE}
    </select>

    <select id="findMonthList" resultType="java.util.Map" parameterType="string">
      select
      CAST(DATE_FORMAT(orderDate,"%d") as SIGNED) date,
      number,
      reservations
      from t_ordersetting
      where
      orderDate like  #{month} "%"
    </select>
</mapper>